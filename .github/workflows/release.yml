name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: read

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    uses: ./.github/workflows/ci.yml

  create-release:
    needs: ci
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Verify tag matches app version
        run: |
          TAG="${{ github.ref_name }}"
          V_TAURI="v$(jq -r '.version' src-tauri/tauri.conf.json)"
          V_PKG="v$(jq -r '.version' package.json)"
          V_CARGO="v$(sed -n 's/^version = "\(.*\)"/\1/p' src-tauri/Cargo.toml | head -1)"
          MISMATCH=""
          [ "$TAG" != "$V_TAURI" ] && MISMATCH="$MISMATCH tauri.conf.json=$V_TAURI"
          [ "$TAG" != "$V_PKG" ]   && MISMATCH="$MISMATCH package.json=$V_PKG"
          [ "$TAG" != "$V_CARGO" ] && MISMATCH="$MISMATCH Cargo.toml=$V_CARGO"
          if [ -n "$MISMATCH" ]; then
            echo "::error::Tag $TAG does not match:$MISMATCH"
            exit 1
          fi

      - name: Create draft release (idempotent)
        run: gh release view "${{ github.ref_name }}" &>/dev/null || gh release create "${{ github.ref_name }}" --draft --generate-notes --title "${{ github.ref_name }}"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  build:
    needs: create-release
    timeout-minutes: 60
    permissions:
      contents: write
      id-token: write
      attestations: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: macos-15
            args: --target universal-apple-darwin
            rust_target: aarch64-apple-darwin,x86_64-apple-darwin
          - platform: ubuntu-22.04
            args: ""
          - platform: windows-2025
            args: ""
    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - uses: pnpm/action-setup@fc06bc1257f339d1d5d8b3a19a8cae5388b55320 # v4

      - uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # v6.3.0
        with:
          node-version: 22
          cache: pnpm

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@efa25f7f19611383d5b0ccf2d1c8914531636bf9 # stable
        with:
          targets: ${{ matrix.rust_target }}

      - name: Install system dependencies (Linux)
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libgtk-3-dev libayatana-appindicator3-dev librsvg2-dev

      - uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32 # v2
        with:
          workspaces: src-tauri

      - run: pnpm install --frozen-lockfile

      - name: Build and upload
        id: tauri
        uses: tauri-apps/tauri-action@84b9d35b5fc46c1e45415bdb6144030364f7ebc5 # v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: ${{ github.ref_name }}
          releaseDraft: true
          args: ${{ matrix.args }}

      - name: Collect artifact files
        id: artifact-files
        shell: bash
        run: |
          {
            echo 'paths<<EOF'
            echo '${{ steps.tauri.outputs.artifactPaths }}' | jq -r '.[]' | while IFS= read -r raw; do
              # Convert Windows backslash paths to forward slashes for Git Bash
              file="${raw//\\//}"
              if [ -f "$file" ]; then
                echo "$file"
              fi
            done
            echo 'EOF'
          } >> "$GITHUB_OUTPUT"

      - name: Generate checksums
        shell: bash
        run: |
          hash_cmd="sha256sum"
          if ! command -v sha256sum &>/dev/null; then
            hash_cmd="shasum -a 256"
          fi
          while IFS= read -r file; do
            [ -n "$file" ] || continue
            (cd "$(dirname "$file")" && $hash_cmd "$(basename "$file")") >> "$RUNNER_TEMP/checksums-${{ matrix.platform }}.txt"
          done <<< "${{ steps.artifact-files.outputs.paths }}"

      - name: Upload checksum artifact
        if: steps.artifact-files.outputs.paths != ''
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7.0.0
        with:
          name: checksums-${{ matrix.platform }}
          path: ${{ runner.temp }}/checksums-${{ matrix.platform }}.txt

      - name: Attest build provenance
        if: steps.artifact-files.outputs.paths != ''
        uses: actions/attest-build-provenance@e8998f949152b193b063cb0ec769d69d929409be # v2
        with:
          subject-path: ${{ steps.artifact-files.outputs.paths }}

  upload-checksums:
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: checksums-*
          merge-multiple: true

      - name: Upload checksums to release
        run: |
          cat checksums-*.txt | sort > SHA256SUMS.txt
          gh release upload "${{ github.ref_name }}" SHA256SUMS.txt --clobber
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPO: ${{ github.repository }}
